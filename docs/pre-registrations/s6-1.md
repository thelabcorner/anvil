# S6-1 PRE-REGISTRATION — v1.2 (research-gate)

> Provenance: authored live in swarm anvil-i7-decode blackboard `deliverable/pre-reg-s6-1`
> (key v3 = text v1.2). Persisted to repo after swarm-store outage. This is the
> binding frozen text for the S6-1 experiment.

ERRATUM (v1.1→v1.2): Amendment-1 tier-2 bound corrected for an arithmetic slip.
v1.1 printed "≤183,223 B / +4.378%"; the formula gives
175,550 × 472,356/452,548 = 183,233.81, trade factor +4.377%. CORRECTED BINDING
BOUND: generated.log bytes ≤ 183,233 B (= FLOOR of baseline × exact Linux trade
factor; rounding rule: floor — conservative for a credit-granting ceiling). The
FORMULA is unchanged from v1.1; only its mis-evaluated integer is fixed. Caught
by linux-ref pre-dependency; no outcome data exists or was consulted.

Frozen: 2026-08-21 04:41 -05:00 · Applies to git HEAD 392e937 + baseline snapshot
tests/benchmark-suite.pre-s61.csv (median-3). Verdict builds land on top; the
flag-OFF control (§5.1) binds a verdict build to this baseline.

TITLE: Whole-codec J-selection + raw-stream budget on mode 15 (hot-op hybrid) —
reproduce the Linux hot-op MECHANISM's trade on Windows references.

## §1 LINEAGE (validated priors — reused, not re-derived)
- EXP. L (FULL PASS, corrected v2): stream suite with ADDITIVE
  J = L_stream + λ·C_decode; J-selection 100% faithful at pre-registered
  λ=0.01 (agreement 660/660 jsonl, 480/480 log); real decode wins at zero ratio
  cost; λ=0.01 is the shipped default.
- EXP. R (PARTIAL PASS): compiled hot-op instruction book, mode 15 — validated
  decode accelerator (1.17–1.34x vs fused m12; log 274 MB/s) at near-preserved
  ratio; ≥2x target NOT met; recorded lever = "I4-3's economics applied to the
  hot-opcode stream itself."
- Prior evidence (Linux, directional — anchor-integrity note per Amendment 2):
  hot-op hybrid + stream-budget sweep measured on an unnamed larger Linux log
  (NOT generated.log), where ANVIL started ~12% AHEAD of q9: ≈452 KB @
  0.87–0.99 GB/s decode vs q9 513 KB @ 0.84 GB/s; encode 33 vs 19.6 MB/s
  (61/61 paired trials). Raw shape-distance-delta stream cost +19.8 KB
  (=+4.377% of the hybrid base) and raised hot-book decode toward ~0.99 GB/s
  (microbench). Linux decode anchors are microbench/CPU-state-sensitive; the
  paired Linux test measured ENCODE only.

## §2 WHAT IS NEW (admin honesty: NOT a new mechanism)
The NEW interaction is WHOLE-CODEC application: per-stream codec choice (incl.
`raw` for the shape-distance-delta stream specifically) decided once at block
level under the decode-cost-weighted objective, instead of per-stream
smallest-wins. This is the recorded remainder of EXP. R. Wire change limited to
stream-codec selection bits (format documents them); no new block mode; old
files keep decoding.

## §3 BINDING CONSTANTS
- Additive J = L_stream + λ·C_decode with λ = 0.01 bytes/μs (EXP. L
  pre-registered value, shipped default). NO new hand-tuned weights; no
  per-stream weight tuning of any kind. μ, ν not re-tuned.
- C_decode SHOULD be size-proportional: measured per-codec cycles/byte × raw_n,
  calibrated from decode-perf's t3 per-stream cost table, FIXED before any
  verdict measurement, calibration table recorded in the ledger. Measured
  calibration ≠ forbidden weight-tuning; OUTCOME-FIT constants remain forbidden.
  If selection stays smallest-wins under size-proportional C_decode, that IS
  the honest result — record it; do not tune constants to force a trade.
- `raw` enabled as a J-candidate for the shape-distance-delta stream (Linux
  stream-budget precedent). All other stream candidates unchanged.
- THRESHOLD-FIT INVALIDATES THE GATE: adjusting any constant after seeing
  results voids the run as a test of this pre-registration.

## §4 FALSIFIABLE TARGET — THE BAR (primary file: generated.log, 1,942,280 B,
median-3, bench arbiter)
Frozen baseline (tests/benchmark-suite.pre-s61.csv, anvil-hotop-rans):
175,550 B (ratio 0.090), decode 273.534 MB/s, encode 19.681 MB/s.
Brotli-q9 reference (same snapshot): 124,669 B (0.064), decode 619.508 MB/s,
encode 29.533 MB/s.

FRONTIER CLAIM PATH (unchanged from v1) — PASS requires ALL THREE legs:
(A) DECODE ≥ 2x mode-15 baseline: generated.log decode ≥ 547.1 MB/s
    (= 2 × 273.534).
(B) RATIO HELD: generated.log bytes ≤ 175,550 B (ratio ≤ 0.090).
(C) FRONTIER LEG: beat brotli-q9 on BOTH planes on generated.log — bytes <
    124,669 B AND decode > 619.508 MB/s — confirmed by tools/pareto_front.py as
    EXTENDS_FRONT on the ratio-vs-decode plane at median-3. That would be the
    FIRST EXTENDS_FRONT row in project history (~396 verdict rows, zero so far).

PRE-REGISTERED OUTCOME TIER (Amendment 1, D1): "FORMULATION-REPRODUCED (partial
credit, NO frontier claim)": leg A PASS ∧ generated.log bytes ≤ 183,233 B
(= floor(175,550 × 472,356/452,548); +4.377%, the Linux-measured raw-mdvar
trade factor; rounding rule floor) ∧ ALL §5 controls PASS ∧ leg C reported
honestly at the achieved ratio. Pre-committed phrasing: "Linux stream-budget
formulation reproduced on Windows: ≥2x decode at ≤+4.377% bytes; frontier not
crossed." B-fail above 183,233 B with A-pass = "trade overpaid" (recorded, no
credit tier).

Secondary (reported, not required for the primary verdict): generated.json
(m15 121,316 B @ 207.133 MB/s) and generated.jsonl (222,381 B @ 288.985) under
the same pattern vs their own baselines; aggregate verdict per pareto tool.

ATTRIBUTION IS PART OF THE VERDICT: did decode move (A)? did ratio hold or what
did the trade cost (B class)? A-pass/C-fail-on-ratio within tier-2 bounds =
formulation reproduced, frontier not crossed (never rounded up). B-fail =
raw-stream trade overpaid (recorded).

## §5 CONTROLS (ALL mandatory before any claim)
1. FLAG-OFF BYTE-IDENTITY: budget disabled ⇒ output byte-exactly equals the
   pre-S6-1 build on ALL corpus files (EXP. L precedent).
2. NO-REGRESSION: random.bin + synth-arith.bin ratio unchanged (~1.0; metric =
   encode speed w/ negative gate); generated.repeat.jsonl bytes ≤ same-build
   exact-LZ baseline row.
3. CORRECTNESS: round-trip verified on all corpus files + fuzzed
   (tests/fuzz.py) BEFORE any number counts.
4. PROTOCOL: median ≥3 reps via anvil_bench; ratio deltas exact (CV 0.000%);
   throughput claims only on files ≥ ~100 KB; verdict ONLY from
   tools/pareto_front.py. NO CLAIM WITHOUT bench's median-3 + pareto verdict.
5. COMPATIBILITY: mode-15 wire delta limited to stream-selection bits; old
   files decode (format signs off).

## §6 SCOPE BOUNDARY (policed during the run)
IN: whole-codec J-selection wiring incl. size-proportional measured C_decode;
raw-candidate for shape-delta stream; stream-selection bits; FORMAT.md docs;
§5 controls.
OUT (smuggling VOIDS the run as an S6-1 test): new parsers; new entropy coders;
hot-op book grammar/RLZ (--hotop-rlzp stays out of the S6-1 verdict build;
Exp-Y lane bugs fixed under Exp-Y's own pre-registration); surprise-budget
retuning; P(d|s) changes; allocator/decode-loop micro-optimization beyond what
stream-materialization selection requires. Anything discovered mid-run → NEW
pre-registration; it does NOT amend this bar.

## §7 HONEST-FAILURE FRAMING (pre-committed)
EXP. R warned decode after stream materialization depends on allocator/setup;
Linux timing "moved with CPU state" and is directional (microbench anchors). If
Windows end-to-end decode economics differ (host per tests/host-spec.md), the
verdict FAILS CLEANLY and is recorded math-vs-implementation-era per working
agreement (EXP. J don't-re-burn treatment). No post-hoc bar adjustment.
ANCHOR-INTEGRITY SENTENCE (mandatory in the written verdict): the Linux
crossing was measured on a different, larger log where ANVIL started ~12%
ahead of q9; Windows generated.log starts 41% behind; this verdict tests the
mechanism's trade on Windows references and neither reproduces nor contradicts
the Linux outcome.

Verdict authority: bench (median-3) + tools/pareto_front.py. Verdict drafting:
research-gate against THIS frozen text only.
Version history: v1 freeze → v1.1 tiers/anchor/calibration amendments
(pre-implementation) → v1.2 arithmetic erratum (bound 183,223→183,233; factor
+4.378%→+4.377%; rounding rule stated).
